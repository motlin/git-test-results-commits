repos:
    - repo: https://github.com/pre-commit/pre-commit-hooks
      rev: v5.0.0
      hooks:
          - id: check-yaml
            args: ["--allow-multiple-documents"]
          - id: check-json
            # Exclude JSONC files (JSON with Comments) as they need comment support
            exclude: "biome\\.jsonc"
          - id: check-toml
          - id: check-xml
          - id: end-of-file-fixer
          - id: trailing-whitespace
          - id: check-added-large-files
            args: ["--maxkb=1000"]
          - id: check-case-conflict
          - id: check-merge-conflict
          - id: detect-private-key
          - id: mixed-line-ending
            args: ["--fix=no"]

    - repo: local
      hooks:
          - id: cargo-fmt
            name: cargo fmt
            entry: cargo +nightly fmt --check
            language: system
            types: [rust]
            pass_filenames: false

          - id: biome
            name: biome
            entry: mise exec -- npx @biomejs/biome format --write
            language: system
            types: [file]
            files: \.(json|jsonc)$

          - id: prettier
            name: prettier
            entry: mise exec -- prettier --write --ignore-unknown
            language: system
            types: [file]
